# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# WoW runtime artefacts (SavedVariables live outside the addon folder, but
# guard against accidental commits if anyone copies them in for debugging)
*.bak
*.lua.bak
SavedVariables/

# Dev-internal docs — must NOT leak into the public repo. AI workflow notes,
# audit backlogs, planning sketches, anything ad-hoc with "private" in the name.
AUDIT.md
PLAN.md
NOTES.md
TODO.md
*.private.md
*.private/
CLAUDE.md
